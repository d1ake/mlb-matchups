name: Daily slate

# Rebuilds data.js with today's stats AND snapshots today's predictions for
# later grading. Cron is UTC; 13:00 UTC ~= 9:00 AM ET. Adjust as needed.
on:
  schedule:
    - cron: '0 13 * * *'
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Build today's data.js
        run: node build-slate.js

      - name: Record today's predictions
        run: node eval/record.js

      - name: Commit if changed
        run: |
          git config user.name  "barreliq-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data.js eval/predictions.jsonl
          if git diff --cached --quiet; then
            echo "Nothing to commit."
          else
            git commit -m "slate + predictions $(date -u +%Y-%m-%d)"
            git push
          fi
